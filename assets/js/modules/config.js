// Configuration centralisée de l'API.
// C'est le SEUL endroit où l'URL de base est définie.
// En production, il suffit de changer cette ligne (ex: "https://mon-back.up.railway.app").
export const API_BASE = "http://localhost:8080";
